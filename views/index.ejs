<%- include('./partials/html-head') %>
<%- include('./partials/nav') %>

<h1> Mind Journal </h1> 

<main>
  <h2>hello, <%= user ? user.profile.name : "friend" %></h2>
  <% if (user?.profile.avatar) { %>
    <div>
      <img 
        src="<%= user.profile.avatar %>" 
        alt="<%= user.profile.name %> avatar"
      >
    </div>
  <% } %>
</main>

<%- include('./partials/footer') %>
